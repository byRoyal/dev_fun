# Small server for COMPUTER-NETWORKS

## Making a docker image (by docker-compose):

```bash
docker-compose up -d --build
```

## Autherising into the users or admin status:

```bash
ssh admin@localhost -p 2222
ssh user1@localhost -p 2222
ssh user2@localhost -p 2222
```


### There is no sudo mode allowed